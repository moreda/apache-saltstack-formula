# TODO: lookup table for additional os_family values

{% set apache = salt['grains.filter_by']({
    'Debian': {
        'pkg': 'apache2',
        'service': 'apache2',
        'config': '/etc/apache2/apache2.conf',
    },
}, merge=salt['pillar.get']('apache:lookup')) %}
